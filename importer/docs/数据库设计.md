# plan阶段：

需要实现的需求：

1. 记录sync job
2. 记录这个sync job对应的所有file task
3. file task需要记录文件同步进度
4. 需要做到插入数据的幂等性
5. 尽量做到高性能
6. 需要兼容其他类型的json

## file task

job id  

status

url

​	entity

​	date

​	part

record_count(用于审计)

read_count(用于审计)

用entity date part作为组合的唯一键，用于insert ignore

last_heartbeat最后一次心跳时间，用于判断是否超时

## sync job

job id

entity

snapshot

created_at

sync job不存储状态字段，因为涉及到可能有空的job，没有指派任何文件，以及后续的job由file task计算得出状态

# worker阶段

